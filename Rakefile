# frozen_string_literal: true

require 'pipeline-tasks'

# Loads a reusable pipeline task set for modern (ephemeral) application pipelines.
load 'pipeline-tasks/rake/app/modern.rake'
load 'stig-processing/rake/security/standard.rake'
